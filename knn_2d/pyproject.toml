[project]
name = "knn-interactive"
version = "0.1.0"
description = "Demo interactivo de k-Nearest Neighbors en 2D."
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.4",
]
