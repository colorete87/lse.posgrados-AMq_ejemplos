# knn_2d Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir `knn_2d/`, un demo interactivo de matplotlib del clasificador kNN en `R²`, según el spec `docs/superpowers/specs/2026-05-09-knn-2d-design.md`.

**Architecture:** Single-file Python script (`knn_interactive.py`) que sigue el patrón de los otros ejemplos del repo (`proceso_gausiano/gp_interactive.py`). Math puro de kNN en funciones top-level testeables (sin matplotlib) y UI armada con `matplotlib.widgets` + `Rectangle`/`fig.text` para los recuadros.

**Tech Stack:** Python 3.13, numpy, matplotlib. Sin scikit-learn (kNN implementado a mano). Sin pytest — verificación inline vía `runpy` + `python -c` siguiendo el patrón de los ejemplos existentes.

---

## Repo conventions to follow

- Cada subcarpeta es un proyecto uv autocontenido con `pyproject.toml` + `uv.lock` propios.
- Single-file script con widgets de matplotlib.
- Layout: panel izquierdo de recuadros con widgets, plot central, panel inferior de sliders + recuadros (kernel/métrica/pesos).
- Funciones `_*` privadas, dict `state` global, helper `_add_group_box(x, y, w, h, label)`.
- Smoke test al final via `MPLBACKEND=Agg uv run python -c "..."` parcheando `plt.show`.
- Render a `/tmp/*.png` para inspección visual.

---

## Task 1: Setup del proyecto

**Files:**
- Create: `knn_2d/`, `knn_2d/pyproject.toml`, `knn_2d/uv.lock`

- [ ] **Step 1: Crear el directorio e inicializar uv**

```bash
mkdir -p knn_2d && cd knn_2d && uv init --name knn-interactive --no-readme --bare
```

- [ ] **Step 2: Agregar dependencias y descripción**

```bash
cd knn_2d && uv add numpy matplotlib
```

Editar `knn_2d/pyproject.toml` para sumar la `description`:

```toml
[project]
name = "knn-interactive"
version = "0.1.0"
description = "Demo interactivo de k-Nearest Neighbors en 2D."
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.4",
]
```

- [ ] **Step 3: Verificar que el venv arma**

Run: `cd knn_2d && uv sync && uv run python -c "import numpy, matplotlib; print('OK')"`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add knn_2d/pyproject.toml knn_2d/uv.lock
git commit -m "knn_2d: setup uv project (numpy, matplotlib)"
```

---

## Task 2: Funciones de distancia

**Files:**
- Create: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Crear el archivo con encabezado, imports y funciones de distancia**

```python
"""
Demo interactivo de k-Nearest Neighbors en 2D.

Funcionalidades:
  - Sliders para # clases (2-6), # train (10-500), # test (5-200), k (1-25).
  - 4 métricas: Euclidiana, Manhattan, Minkowski (p configurable),
    Mahalanobis (Σ global del train).
  - 3 pesos: uniforme, 1/d, gaussiano (h configurable).
  - 3 botones de evaluación: click (single point con vecinos visibles),
    test completo (accuracy en X_test), 5-fold CV.
  - Lista de scores acumulados, regenerar datos, reset todo.
  - Toggle de la región de decisión sobre todo el plano.

Ejecutar:
    uv run python knn_interactive.py
"""

import numpy as np
import matplotlib.pyplot as plt
from matplotlib.patches import Rectangle
from matplotlib.widgets import Slider, RadioButtons, Button, CheckButtons


RNG = np.random.default_rng()


# ===========================================================
# Distancias (X1: (n1, 2), X2: (n2, 2) -> matriz (n1, n2))
# ===========================================================
def _dist_euclidean(X1, X2):
    diff = X1[:, None, :] - X2[None, :, :]
    return np.sqrt(np.sum(diff ** 2, axis=-1))


def _dist_manhattan(X1, X2):
    diff = X1[:, None, :] - X2[None, :, :]
    return np.sum(np.abs(diff), axis=-1)


def _dist_minkowski(X1, X2, p=3.0):
    diff = X1[:, None, :] - X2[None, :, :]
    return np.sum(np.abs(diff) ** p, axis=-1) ** (1.0 / p)


def _dist_mahalanobis(X1, X2, VI):
    diff = X1[:, None, :] - X2[None, :, :]   # (n1, n2, 2)
    # cuadrática de Mahalanobis: diff @ VI @ diffᵀ por par
    inner = np.einsum('ijk,kl,ijl->ij', diff, VI, diff)
    return np.sqrt(np.maximum(inner, 0.0))
```

- [ ] **Step 2: Verificar las 4 distancias inline**

Run:
```bash
cd knn_2d && uv run python -c "
import numpy as np
import importlib.util
spec = importlib.util.spec_from_file_location('mod', 'knn_interactive.py')
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)

X1 = np.array([[0.0, 0.0], [3.0, 4.0]])
X2 = np.array([[0.0, 0.0], [1.0, 0.0]])

de = mod._dist_euclidean(X1, X2)
assert de.shape == (2, 2)
assert np.allclose(de, [[0.0, 1.0], [5.0, np.sqrt(20)]]), de
print('euclid OK')

dm = mod._dist_manhattan(X1, X2)
assert np.allclose(dm, [[0.0, 1.0], [7.0, 6.0]]), dm
print('manhattan OK')

dmk = mod._dist_minkowski(X1, X2, p=3.0)
# minkowski p=3: cbrt(|dx|^3 + |dy|^3)
assert np.allclose(dmk[1, 0], (3.0**3 + 4.0**3) ** (1/3.0)), dmk
print('minkowski OK')

VI = np.array([[1.0, 0.0], [0.0, 1.0]])  # identidad => mahal = euclid
dma = mod._dist_mahalanobis(X1, X2, VI)
assert np.allclose(dma, de), (dma, de)
print('mahalanobis OK')

print('TODO OK')
"
```
Expected: `TODO OK`

- [ ] **Step 3: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: distancias (euclid, manhattan, minkowski, mahalanobis)"
```

---

## Task 3: Funciones de peso

**Files:**
- Modify: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Agregar las 3 funciones de peso después de las distancias**

```python
# ===========================================================
# Pesos (d: array, devuelve mismos shape con weights >= 0)
# ===========================================================
def _w_uniform(d):
    return np.ones_like(d)


def _w_inv_dist(d, eps=1e-9):
    return 1.0 / (d + eps)


def _w_gaussian(d, h=1.0):
    return np.exp(-0.5 * (d / h) ** 2)
```

- [ ] **Step 2: Verificar pesos inline**

Run:
```bash
cd knn_2d && uv run python -c "
import numpy as np
import importlib.util
spec = importlib.util.spec_from_file_location('mod', 'knn_interactive.py')
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)

d = np.array([0.0, 1.0, 2.0])
assert np.allclose(mod._w_uniform(d), [1, 1, 1])
print('uniform OK')

# 1/d a 0.0 debe ser muy grande pero finito
w = mod._w_inv_dist(d)
assert w[0] > 1e8 and w[1] == 1.0 and np.isclose(w[2], 0.5), w
print('1/d OK')

# gaussiano: w(0)=1, decae con d
g = mod._w_gaussian(d, h=1.0)
assert g[0] == 1.0 and g[1] < g[0] and g[2] < g[1], g
print('gaussian OK')

print('TODO OK')
"
```
Expected: `TODO OK`

- [ ] **Step 3: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: funciones de peso (uniform, 1/d, gaussiano)"
```

---

## Task 4: predict_knn

**Files:**
- Modify: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Agregar predict_knn después de los pesos**

```python
# ===========================================================
# Predicción kNN
# ===========================================================
def predict_knn(X_query, X_train, y_train, n_classes, k,
                metric_fn, weight_fn):
    """
    X_query:    (n_query, 2)
    X_train:    (n_train, 2)
    y_train:    (n_train,) enteros 0..n_classes-1
    metric_fn:  f(X1, X2) -> (n1, n2) matriz de distancias
    weight_fn:  f(d) -> array de pesos del mismo shape

    Devuelve:
      pred:       (n_query,) clase predicha
      confidence: (n_query,) peso del voto ganador / suma de pesos
      neighbors:  (n_query, k) índices en X_train de los k vecinos
                  más cercanos (ordenados por distancia)
    """
    X_query = np.atleast_2d(X_query).astype(float)
    n_train = len(X_train)
    k_eff = min(k, n_train)

    D = metric_fn(X_query, X_train)             # (n_query, n_train)
    # k vecinos: índices de los k menores por fila
    idx_part = np.argpartition(D, kth=k_eff - 1, axis=1)[:, :k_eff]
    # ordenarlos por distancia para que neighbors[0] sea el más cercano
    rows = np.arange(len(X_query))[:, None]
    sort_within = np.argsort(D[rows, idx_part], axis=1)
    neighbors = idx_part[rows, sort_within]     # (n_query, k_eff)
    d_nbr = D[rows, neighbors]
    w_nbr = weight_fn(d_nbr)                    # (n_query, k_eff)
    cls_nbr = y_train[neighbors]                # (n_query, k_eff)

    # voto ponderado: sumar pesos por clase
    votes = np.zeros((len(X_query), n_classes))
    for c in range(n_classes):
        mask = (cls_nbr == c)
        votes[:, c] = np.sum(w_nbr * mask, axis=1)

    pred = np.argmax(votes, axis=1)             # empate => menor índice (np.argmax)
    total = np.sum(votes, axis=1)
    confidence = np.where(total > 0,
                          votes[np.arange(len(X_query)), pred] / np.maximum(total, 1e-12),
                          0.0)
    return pred, confidence, neighbors
```

- [ ] **Step 2: Verificar predict_knn con casos canónicos**

Run:
```bash
cd knn_2d && uv run python -c "
import numpy as np
import importlib.util
spec = importlib.util.spec_from_file_location('mod', 'knn_interactive.py')
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)

# Caso 1: 1NN, query exacto sobre un punto de train -> predice su clase
X_train = np.array([[0.0, 0.0], [5.0, 5.0]])
y_train = np.array([0, 1])
X_query = np.array([[0.0, 0.0], [5.0, 5.0]])
pred, conf, nbr = mod.predict_knn(X_query, X_train, y_train, 2, 1,
                                  mod._dist_euclidean, mod._w_uniform)
assert list(pred) == [0, 1], pred
assert np.allclose(conf, [1.0, 1.0]), conf
print('1NN coincidente OK')

# Caso 2: 3-NN simple, query equidistante de 2 clases pero más vecinos clase 0
X_train = np.array([[0.0, 0.0], [0.0, 0.5], [0.0, -0.5], [3.0, 0.0]])
y_train = np.array([0, 0, 0, 1])
pred, conf, nbr = mod.predict_knn(np.array([[0.0, 0.0]]), X_train, y_train,
                                  2, 3, mod._dist_euclidean, mod._w_uniform)
assert pred[0] == 0, pred
print('3-NN voto mayoría OK')

# Caso 3: k > n_train debe degradarse a usar todos los puntos sin error
pred, conf, nbr = mod.predict_knn(np.array([[1.0, 1.0]]), X_train, y_train,
                                  2, 100, mod._dist_euclidean, mod._w_uniform)
assert nbr.shape == (1, 4), nbr.shape
print('clamp k > n_train OK')

print('TODO OK')
"
```
Expected: `TODO OK`

- [ ] **Step 3: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: predict_knn con voto ponderado"
```

---

## Task 5: Generación de datos

**Files:**
- Modify: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Agregar make_random_dataset después de predict_knn**

```python
# ===========================================================
# Generación de datos (n_classes clusters gaussianos 2D)
# ===========================================================
def _random_means(n_classes, rng, x_min=-3.5, x_max=3.5, min_sep=2.0,
                  max_tries=200):
    """Sortea medias para n_classes clusters separadas al menos min_sep."""
    means = []
    tries = 0
    while len(means) < n_classes and tries < max_tries:
        m = rng.uniform(x_min, x_max, size=2)
        if all(np.linalg.norm(m - mm) >= min_sep for mm in means):
            means.append(m)
        tries += 1
    # fallback: si no logra separación, completa con grid uniforme
    while len(means) < n_classes:
        means.append(rng.uniform(x_min, x_max, size=2))
    return np.array(means)


def _random_cov(rng, sigma_lo=0.4, sigma_hi=1.0, rho_max=0.6):
    sx = rng.uniform(sigma_lo, sigma_hi)
    sy = rng.uniform(sigma_lo, sigma_hi)
    rho = rng.uniform(-rho_max, rho_max)
    return np.array([[sx ** 2,        rho * sx * sy],
                     [rho * sx * sy,  sy ** 2]])


def make_random_dataset(n_classes, n_train, n_test, rng=None):
    rng = rng or RNG
    means = _random_means(n_classes, rng)
    covs = [_random_cov(rng) for _ in range(n_classes)]
    Ls = [np.linalg.cholesky(c) for c in covs]

    def sample_n(per_class):
        Xs, ys = [], []
        for c in range(n_classes):
            n = per_class[c]
            if n == 0:
                continue
            z = rng.standard_normal((n, 2))
            Xs.append(z @ Ls[c].T + means[c])
            ys.append(np.full(n, c, dtype=int))
        return np.concatenate(Xs), np.concatenate(ys)

    def split(total):
        base, rem = divmod(total, n_classes)
        return [base + (1 if i < rem else 0) for i in range(n_classes)]

    X_train, y_train = sample_n(split(n_train))
    X_test, y_test = sample_n(split(n_test))

    # mezclar para no quedar con clases ordenadas
    perm_tr = rng.permutation(len(X_train))
    perm_te = rng.permutation(len(X_test))
    return (X_train[perm_tr], y_train[perm_tr],
            X_test[perm_te], y_test[perm_te])
```

- [ ] **Step 2: Verificar make_random_dataset**

Run:
```bash
cd knn_2d && uv run python -c "
import numpy as np
import importlib.util
spec = importlib.util.spec_from_file_location('mod', 'knn_interactive.py')
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)

rng = np.random.default_rng(42)
Xtr, ytr, Xte, yte = mod.make_random_dataset(3, 30, 12, rng)
assert Xtr.shape == (30, 2) and ytr.shape == (30,)
assert Xte.shape == (12, 2) and yte.shape == (12,)
# todas las clases presentes y splits parejos (con resto distribuido)
counts_tr = np.bincount(ytr, minlength=3)
counts_te = np.bincount(yte, minlength=3)
assert np.all(counts_tr >= 30 // 3) and np.all(counts_tr <= 30 // 3 + 1), counts_tr
assert np.all(counts_te >= 12 // 3) and counts_te.sum() == 12, counts_te
print(f'train counts: {counts_tr}, test counts: {counts_te}')
print('TODO OK')
"
```
Expected: `TODO OK` con counts balanceados.

- [ ] **Step 3: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: make_random_dataset (clusters gaussianos por clase)"
```

---

## Task 6: K-fold cross-validation

**Files:**
- Modify: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Agregar k_fold_score después de make_random_dataset**

```python
# ===========================================================
# K-fold CV (5-fold fijo) sobre el train. Devuelve accuracy promedio.
# ===========================================================
def k_fold_score(X_train, y_train, n_classes, k, metric_fn, weight_fn,
                 n_folds=5, rng=None):
    rng = rng or RNG
    n = len(X_train)
    if n < n_folds:
        return float("nan")
    perm = rng.permutation(n)
    folds = np.array_split(perm, n_folds)
    accs = []
    for i in range(n_folds):
        val_idx = folds[i]
        tr_idx = np.concatenate([folds[j] for j in range(n_folds) if j != i])
        if len(tr_idx) == 0:
            continue
        pred, _, _ = predict_knn(X_train[val_idx], X_train[tr_idx],
                                 y_train[tr_idx], n_classes, k,
                                 metric_fn, weight_fn)
        accs.append(np.mean(pred == y_train[val_idx]))
    return float(np.mean(accs))
```

- [ ] **Step 2: Verificar k_fold_score**

Run:
```bash
cd knn_2d && uv run python -c "
import numpy as np
import importlib.util
spec = importlib.util.spec_from_file_location('mod', 'knn_interactive.py')
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)

rng = np.random.default_rng(0)
Xtr, ytr, _, _ = mod.make_random_dataset(3, 60, 0, rng)
acc = mod.k_fold_score(Xtr, ytr, 3, 5, mod._dist_euclidean, mod._w_uniform,
                       n_folds=5, rng=rng)
print(f'5-fold acc: {acc:.3f}')
assert 0.0 <= acc <= 1.0
# clusters gaussianos separados deberían dar accuracy alta
assert acc > 0.6, acc
print('TODO OK')
"
```
Expected: accuracy alta (> 0.6) y `TODO OK`.

- [ ] **Step 3: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: k_fold_score (5-fold CV)"
```

---

## Task 7: Estado, figura, layout base, render del dataset

**Files:**
- Modify: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Agregar registries de métricas/pesos, defaults, y estado global**

Después de `k_fold_score`:

```python
# ===========================================================
# Registries y defaults
# ===========================================================
METRICS = ["Euclidiana", "Manhattan", "Minkowski", "Mahalanobis"]
WEIGHTS = ["uniform", "1/d", "gaussiano"]


def _build_metric_fn(name, X_train, p, mahal_VI):
    if name == "Euclidiana":
        return _dist_euclidean
    if name == "Manhattan":
        return _dist_manhattan
    if name == "Minkowski":
        return lambda X1, X2: _dist_minkowski(X1, X2, p=p)
    if name == "Mahalanobis":
        return lambda X1, X2: _dist_mahalanobis(X1, X2, VI=mahal_VI)
    raise ValueError(name)


def _build_weight_fn(name, h):
    if name == "uniform":
        return _w_uniform
    if name == "1/d":
        return _w_inv_dist
    if name == "gaussiano":
        return lambda d: _w_gaussian(d, h=h)
    raise ValueError(name)


def _compute_mahal_VI(X_train):
    if len(X_train) < 3:
        return np.eye(2)
    cov = np.cov(X_train.T) + 1e-6 * np.eye(2)
    try:
        return np.linalg.inv(cov)
    except np.linalg.LinAlgError:
        return np.eye(2)


X_MIN, X_MAX = -5.0, 5.0
GRID = 60
gxs = np.linspace(X_MIN, X_MAX, GRID)
GX1, GX2 = np.meshgrid(gxs, gxs)
GRID_PTS = np.column_stack([GX1.ravel(), GX2.ravel()])

DEFAULTS = {
    "n_classes": 3,
    "n_train": 150,
    "n_test": 50,
    "k": 5,
    "metric": "Euclidiana",
    "minkowski_p": 3.0,
    "weights": "uniform",
    "gaussian_h": 1.0,
    "show_decision": True,
    "show_test": True,
}

state = dict(DEFAULTS)
state.update({
    "X_train": np.empty((0, 2)),
    "y_train": np.empty(0, dtype=int),
    "X_test": np.empty((0, 2)),
    "y_test": np.empty(0, dtype=int),
    "y_test_pred": None,
    "mahal_VI": np.eye(2),
    "scores": [],          # list[dict]
    "last_query": None,    # dict | None
})


def _regenerate_data():
    """Resortea X_train, y_train, X_test, y_test y recalcula mahal_VI.
    No toca scores ni last_query (eso lo hace el caller)."""
    Xtr, ytr, Xte, yte = make_random_dataset(
        state["n_classes"], state["n_train"], state["n_test"], RNG)
    state["X_train"] = Xtr
    state["y_train"] = ytr
    state["X_test"] = Xte
    state["y_test"] = yte
    state["y_test_pred"] = None
    state["mahal_VI"] = _compute_mahal_VI(Xtr)
```

- [ ] **Step 2: Agregar la figura, axes, helper de recuadros y placeholder de redraw**

```python
# ===========================================================
# Figura
# ===========================================================
fig = plt.figure(figsize=(13, 7.5))
ax = fig.add_axes([0.27, 0.36, 0.50, 0.58])
ax_scores = fig.add_axes([0.80, 0.36, 0.18, 0.58])
ax_scores.set_xticks([])
ax_scores.set_yticks([])
ax_scores.set_facecolor("#fafafa")
ax_scores.set_title("Scores", fontsize=10, weight="bold")

ax.set_aspect("equal")
ax.set_xlim(X_MIN, X_MAX)
ax.set_ylim(X_MIN, X_MAX)
ax.set_xlabel("$x_1$")
ax.set_ylabel("$x_2$")
ax.grid(alpha=0.3)
ax.set_title("k-NN interactivo (2D)", loc="left")

CLASS_COLORS = ["#1f77b4", "#ff7f0e", "#2ca02c", "#d62728", "#9467bd", "#8c564b"]


def _add_group_box(x, y, w, h, label):
    rect = Rectangle((x, y), w, h, transform=fig.transFigure,
                     linewidth=0.9, edgecolor="#666666",
                     facecolor="none", zorder=0)
    fig.add_artist(rect)
    return fig.text(x + w / 2, y + h + 0.006, label,
                    ha="center", va="bottom", fontsize=9, weight="bold",
                    color="#333333")


# Artistas que reusamos en cada redraw
train_scatter = ax.scatter([], [], s=40, edgecolors="black", linewidths=0.5)
test_scatter = ax.scatter([], [], s=40, facecolors="none", linewidths=1.2)
query_marker, = ax.plot([], [], "x", color="black", ms=14, mew=2.5, zorder=12)
neighbor_lines = []   # lista de artistas Line2D que conectan query con vecinos
decision_image = [None]  # AxesImage opcional


def _safe_remove(artist):
    try:
        artist.remove()
    except Exception:
        pass


def _clear_neighbor_lines():
    for ln in neighbor_lines:
        _safe_remove(ln)
    neighbor_lines.clear()


def redraw():
    # train + test
    if len(state["X_train"]) > 0:
        colors_tr = [CLASS_COLORS[c] for c in state["y_train"]]
        train_scatter.set_offsets(state["X_train"])
        train_scatter.set_facecolors(colors_tr)
    else:
        train_scatter.set_offsets(np.empty((0, 2)))

    if state["show_test"] and len(state["X_test"]) > 0:
        edge_colors = [CLASS_COLORS[c] for c in state["y_test"]]
        test_scatter.set_offsets(state["X_test"])
        test_scatter.set_edgecolors(edge_colors)
    else:
        test_scatter.set_offsets(np.empty((0, 2)))

    fig.canvas.draw_idle()


_regenerate_data()
redraw()

if __name__ == "__main__":
    plt.show()
```

- [ ] **Step 3: Smoke test del render inicial**

Run:
```bash
cd knn_2d && MPLBACKEND=Agg uv run python -c "
import matplotlib.pyplot as plt
captured = {}
def fake(*a, **k): captured['fig'] = plt.gcf()
plt.show = fake
import runpy
ns = runpy.run_path('knn_interactive.py', run_name='__main__')
ns['fig'].savefig('/tmp/knn_step7.png', dpi=110)
print(f'train={ns[\"state\"][\"X_train\"].shape}, test={ns[\"state\"][\"X_test\"].shape}')
print('saved /tmp/knn_step7.png')
"
```
Expected: shapes `(150, 2)` y `(50, 2)`. Render inicial muestra train (sólidos) y test (huecos) coloreados por clase.

- [ ] **Step 4: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: figura, layout base y render del dataset"
```

---

## Task 8: Sliders básicos (# clases, # train, # test, k)

**Files:**
- Modify: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Agregar sliders en la zona inferior justo antes de `if __name__ == "__main__":`**

```python
# ===========================================================
# Sliders básicos (# clases, # train, # test, k)
# ===========================================================
ax_n_classes = plt.axes([0.06, 0.26, 0.18, 0.020])
sl_n_classes = Slider(ax_n_classes, "# clases", 2, 6, valinit=DEFAULTS["n_classes"],
                       valstep=1)

ax_n_train = plt.axes([0.06, 0.21, 0.18, 0.020])
sl_n_train = Slider(ax_n_train, "# train", 10, 500, valinit=DEFAULTS["n_train"],
                     valstep=10)

ax_n_test = plt.axes([0.06, 0.16, 0.18, 0.020])
sl_n_test = Slider(ax_n_test, "# test", 5, 200, valinit=DEFAULTS["n_test"],
                    valstep=5)

ax_k = plt.axes([0.06, 0.11, 0.18, 0.020])
sl_k = Slider(ax_k, "k", 1, 25, valinit=DEFAULTS["k"], valstep=1)


def _on_data_slider(_v):
    state["n_classes"] = int(sl_n_classes.val)
    state["n_train"] = int(sl_n_train.val)
    state["n_test"] = int(sl_n_test.val)
    _regenerate_data()
    state["scores"].clear()
    state["last_query"] = None
    redraw()


def _on_k(_v):
    state["k"] = int(sl_k.val)
    redraw()


sl_n_classes.on_changed(_on_data_slider)
sl_n_train.on_changed(_on_data_slider)
sl_n_test.on_changed(_on_data_slider)
sl_k.on_changed(_on_k)
```

- [ ] **Step 2: Smoke test que los sliders disparan el regen**

Run:
```bash
cd knn_2d && MPLBACKEND=Agg uv run python -c "
import matplotlib.pyplot as plt
plt.show = lambda *a, **k: None
import runpy
ns = runpy.run_path('knn_interactive.py', run_name='__main__')
state = ns['state']
ns['_on_data_slider'](None)
assert state['n_classes'] == 3 and state['X_train'].shape == (150, 2)
ns['sl_n_classes'].set_val(5)
assert state['n_classes'] == 5
print('sliders OK')
"
```
Expected: `sliders OK`.

- [ ] **Step 3: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: sliders de # clases, # train, # test, k"
```

---

## Task 9: Recuadros Métrica y Pesos

**Files:**
- Modify: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Agregar después de los sliders básicos**

```python
# ===========================================================
# Recuadro Métrica
# ===========================================================
_add_group_box(0.30, 0.04, 0.32, 0.255, "Métrica")
ax_metric = plt.axes([0.31, 0.07, 0.10, 0.20])
radio_metric = RadioButtons(ax_metric, METRICS,
                             active=METRICS.index(DEFAULTS["metric"]))

ax_p = plt.axes([0.45, 0.16, 0.15, 0.020])
sl_p = Slider(ax_p, "p (Mink.)", 1.0, 5.0, valinit=DEFAULTS["minkowski_p"])
sl_p.set_active(DEFAULTS["metric"] == "Minkowski")


def _on_metric(label):
    state["metric"] = label
    sl_p.set_active(label == "Minkowski")
    if label == "Mahalanobis":
        state["mahal_VI"] = _compute_mahal_VI(state["X_train"])
    redraw()


def _on_p(v):
    state["minkowski_p"] = float(v)
    if state["metric"] == "Minkowski":
        redraw()


radio_metric.on_clicked(_on_metric)
sl_p.on_changed(_on_p)


# ===========================================================
# Recuadro Pesos
# ===========================================================
_add_group_box(0.65, 0.04, 0.32, 0.255, "Pesos")
ax_weights = plt.axes([0.66, 0.07, 0.10, 0.20])
radio_weights = RadioButtons(ax_weights, WEIGHTS,
                              active=WEIGHTS.index(DEFAULTS["weights"]))

ax_h = plt.axes([0.80, 0.16, 0.15, 0.020])
sl_h = Slider(ax_h, "h (gauss)", 0.1, 3.0, valinit=DEFAULTS["gaussian_h"])
sl_h.set_active(DEFAULTS["weights"] == "gaussiano")


def _on_weights(label):
    state["weights"] = label
    sl_h.set_active(label == "gaussiano")
    redraw()


def _on_h(v):
    state["gaussian_h"] = float(v)
    if state["weights"] == "gaussiano":
        redraw()


radio_weights.on_clicked(_on_weights)
sl_h.on_changed(_on_h)
```

- [ ] **Step 2: Smoke test que cambiar métrica/pesos no rompe**

Run:
```bash
cd knn_2d && MPLBACKEND=Agg uv run python -c "
import matplotlib.pyplot as plt
plt.show = lambda *a, **k: None
import runpy
ns = runpy.run_path('knn_interactive.py', run_name='__main__')
state = ns['state']
for m in ns['METRICS']:
    ns['_on_metric'](m)
    assert state['metric'] == m
for w in ns['WEIGHTS']:
    ns['_on_weights'](w)
    assert state['weights'] == w
ns['_on_p'](2.5); ns['_on_h'](0.7)
assert state['minkowski_p'] == 2.5 and state['gaussian_h'] == 0.7
print('metric/weights OK')
"
```
Expected: `metric/weights OK`.

- [ ] **Step 3: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: recuadros Métrica y Pesos con sliders dependientes"
```

---

## Task 10: Región de decisión + checkboxes Vista

**Files:**
- Modify: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Agregar el bloque "Vista" en el panel izquierdo (antes del recuadro Métrica)**

```python
# ===========================================================
# Recuadro Vista
# ===========================================================
_add_group_box(0.025, 0.83, 0.21, 0.10, "Vista")
ax_check = plt.axes([0.04, 0.835, 0.18, 0.085])
ax_check.set_facecolor("none")
chk = CheckButtons(ax_check,
                    ["Mostrar región de decisión", "Mostrar test points"],
                    actives=[DEFAULTS["show_decision"], DEFAULTS["show_test"]])


def _on_check(label):
    if label == "Mostrar región de decisión":
        state["show_decision"] = not state["show_decision"]
    elif label == "Mostrar test points":
        state["show_test"] = not state["show_test"]
    redraw()


chk.on_clicked(_on_check)
```

- [ ] **Step 2: Sumar el cómputo y dibujado de la región de decisión en `redraw()`**

Reemplazar la versión actual de `redraw()` por:

```python
def redraw():
    # 1. Región de decisión
    _safe_remove(decision_image[0])
    decision_image[0] = None
    if state["show_decision"] and len(state["X_train"]) > 0:
        metric_fn = _build_metric_fn(state["metric"], state["X_train"],
                                      state["minkowski_p"], state["mahal_VI"])
        weight_fn = _build_weight_fn(state["weights"], state["gaussian_h"])
        pred_grid, _, _ = predict_knn(GRID_PTS, state["X_train"], state["y_train"],
                                       state["n_classes"], state["k"],
                                       metric_fn, weight_fn)
        # imshow con colormap personalizado por clase
        from matplotlib.colors import ListedColormap
        cmap = ListedColormap(CLASS_COLORS[:state["n_classes"]])
        decision_image[0] = ax.imshow(
            pred_grid.reshape(GRID, GRID),
            extent=(X_MIN, X_MAX, X_MIN, X_MAX),
            origin="lower", cmap=cmap, alpha=0.18,
            vmin=0, vmax=state["n_classes"] - 1, interpolation="nearest", zorder=0)

    # 2. Train + test
    if len(state["X_train"]) > 0:
        colors_tr = [CLASS_COLORS[c] for c in state["y_train"]]
        train_scatter.set_offsets(state["X_train"])
        train_scatter.set_facecolors(colors_tr)
    else:
        train_scatter.set_offsets(np.empty((0, 2)))

    if state["show_test"] and len(state["X_test"]) > 0:
        edge_colors = [CLASS_COLORS[c] for c in state["y_test"]]
        test_scatter.set_offsets(state["X_test"])
        test_scatter.set_edgecolors(edge_colors)
    else:
        test_scatter.set_offsets(np.empty((0, 2)))

    fig.canvas.draw_idle()
```

- [ ] **Step 3: Smoke test del render con región de decisión**

Run:
```bash
cd knn_2d && MPLBACKEND=Agg uv run python -c "
import matplotlib.pyplot as plt
captured = {}
def fake(*a, **k): captured['fig'] = plt.gcf()
plt.show = fake
import runpy
ns = runpy.run_path('knn_interactive.py', run_name='__main__')
captured['fig'].savefig('/tmp/knn_step10_on.png', dpi=110)
ns['_on_check']('Mostrar región de decisión')
captured['fig'].savefig('/tmp/knn_step10_off.png', dpi=110)
print('renders saved')
"
```
Expected: dos PNGs, el primero con shading de regiones, el segundo sin él.

- [ ] **Step 4: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: región de decisión + checkboxes Vista"
```

---

## Task 11: Single-point evaluation (click handler)

**Files:**
- Modify: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Agregar handler de click después del bloque Vista**

```python
# ===========================================================
# Click handler: evalúa un punto, dibuja vecinos y caption
# ===========================================================
def on_click(event):
    if event.inaxes != ax:
        return
    if event.xdata is None or event.ydata is None:
        return
    x = float(event.xdata); y = float(event.ydata)
    metric_fn = _build_metric_fn(state["metric"], state["X_train"],
                                  state["minkowski_p"], state["mahal_VI"])
    weight_fn = _build_weight_fn(state["weights"], state["gaussian_h"])
    pred, conf, nbr = predict_knn(np.array([[x, y]]), state["X_train"],
                                   state["y_train"], state["n_classes"],
                                   state["k"], metric_fn, weight_fn)
    state["last_query"] = {
        "x": x, "y": y,
        "pred": int(pred[0]),
        "conf": float(conf[0]),
        "neighbors": nbr[0].tolist(),
    }
    redraw()


fig.canvas.mpl_connect("button_press_event", on_click)
```

- [ ] **Step 2: Sumar dibujado de query y vecinos al final de `redraw()` (antes de `fig.canvas.draw_idle()`)**

Insertar:

```python
    # 3. Query y vecinos del último click
    _clear_neighbor_lines()
    if state["last_query"] is not None and len(state["X_train"]) > 0:
        q = state["last_query"]
        query_marker.set_data([q["x"]], [q["y"]])
        for idx in q["neighbors"]:
            xs = [q["x"], state["X_train"][idx, 0]]
            ys = [q["y"], state["X_train"][idx, 1]]
            ln, = ax.plot(xs, ys, color="black", lw=0.7, alpha=0.6, zorder=11)
            neighbor_lines.append(ln)
        ax.set_title(
            f"k-NN interactivo (2D)  —  query=({q['x']:+.2f}, {q['y']:+.2f})  "
            f"pred=clase {q['pred']}  conf={q['conf']:.2f}",
            loc="left", fontsize=10)
    else:
        query_marker.set_data([], [])
        ax.set_title("k-NN interactivo (2D)", loc="left", fontsize=10)
```

- [ ] **Step 3: Smoke test simulando un click**

Run:
```bash
cd knn_2d && MPLBACKEND=Agg uv run python -c "
import matplotlib.pyplot as plt
captured = {}
def fake(*a, **k): captured['fig'] = plt.gcf()
plt.show = fake
import runpy
ns = runpy.run_path('knn_interactive.py', run_name='__main__')

class E: pass
e = E(); e.inaxes = ns['ax']; e.xdata = 0.0; e.ydata = 0.0; e.button = 1
ns['on_click'](e)
q = ns['state']['last_query']
assert q is not None and 0 <= q['pred'] < 3
assert len(q['neighbors']) == 5  # k=5 por default
print(f'query: pred={q[\"pred\"]} conf={q[\"conf\"]:.2f} k_nbr={len(q[\"neighbors\"])}')
captured['fig'].savefig('/tmp/knn_step11.png', dpi=110)
print('saved')
"
```
Expected: query con prediction válida y 5 vecinos visibles en el render.

- [ ] **Step 4: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: click handler para single-point evaluation"
```

---

## Task 12: Botones Test completo y K-fold + scores

**Files:**
- Modify: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Agregar el recuadro Evaluación con los 3 botones**

Después del recuadro Vista:

```python
# ===========================================================
# Recuadro Evaluación
# ===========================================================
_add_group_box(0.025, 0.55, 0.21, 0.255, "Evaluación")

ax_btn_click = plt.axes([0.04, 0.74, 0.18, 0.04])
btn_click = Button(ax_btn_click, "Eval. punto (click)",
                    color="#cde7ff", hovercolor="#a8d2f8")

ax_btn_test = plt.axes([0.04, 0.68, 0.18, 0.04])
btn_test = Button(ax_btn_test, "Test completo",
                   color="#ffd8c2", hovercolor="#f8b690")

ax_btn_kfold = plt.axes([0.04, 0.62, 0.18, 0.04])
btn_kfold = Button(ax_btn_kfold, "K-fold CV (5)",
                    color="#fff0b0", hovercolor="#f5dc73")


def _on_btn_click(_event):
    # No hace nada: el click handler ya está siempre activo. El botón es
    # un recordatorio visual + lugar reservado para un futuro modo "armado".
    pass


def _on_btn_test(_event):
    if len(state["X_test"]) == 0 or len(state["X_train"]) == 0:
        return
    metric_fn = _build_metric_fn(state["metric"], state["X_train"],
                                  state["minkowski_p"], state["mahal_VI"])
    weight_fn = _build_weight_fn(state["weights"], state["gaussian_h"])
    pred, _, _ = predict_knn(state["X_test"], state["X_train"], state["y_train"],
                              state["n_classes"], state["k"],
                              metric_fn, weight_fn)
    acc = float(np.mean(pred == state["y_test"]))
    state["y_test_pred"] = pred
    state["scores"].insert(0, {
        "type": "test", "k": state["k"],
        "metric": state["metric"], "weights": state["weights"],
        "score": acc,
    })
    redraw()


def _on_btn_kfold(_event):
    if len(state["X_train"]) < 5:
        return
    metric_fn = _build_metric_fn(state["metric"], state["X_train"],
                                  state["minkowski_p"], state["mahal_VI"])
    weight_fn = _build_weight_fn(state["weights"], state["gaussian_h"])
    acc = k_fold_score(state["X_train"], state["y_train"],
                       state["n_classes"], state["k"],
                       metric_fn, weight_fn, n_folds=5, rng=RNG)
    state["scores"].insert(0, {
        "type": "kfold", "k": state["k"],
        "metric": state["metric"], "weights": state["weights"],
        "score": acc,
    })
    redraw()


btn_click.on_clicked(_on_btn_click)
btn_test.on_clicked(_on_btn_test)
btn_kfold.on_clicked(_on_btn_kfold)
```

- [ ] **Step 2: En `redraw()`, sumar coloreado de bordes según acierto cuando hay `y_test_pred`**

Reemplazar el bloque actual de test_scatter por:

```python
    if state["show_test"] and len(state["X_test"]) > 0:
        face_colors = [CLASS_COLORS[c] for c in state["y_test"]]
        if state["y_test_pred"] is not None:
            edge_colors = ["#2ca02c" if p == y else "#d62728"
                           for p, y in zip(state["y_test_pred"], state["y_test"])]
            edge_lw = 1.6
        else:
            edge_colors = face_colors
            edge_lw = 1.2
        test_scatter.set_offsets(state["X_test"])
        test_scatter.set_facecolors("none")
        test_scatter.set_edgecolors(edge_colors)
        test_scatter.set_linewidths(edge_lw)
    else:
        test_scatter.set_offsets(np.empty((0, 2)))
```

- [ ] **Step 3: Smoke test de los 3 botones de evaluación**

Run:
```bash
cd knn_2d && MPLBACKEND=Agg uv run python -c "
import matplotlib.pyplot as plt
captured = {}
def fake(*a, **k): captured['fig'] = plt.gcf()
plt.show = fake
import runpy
ns = runpy.run_path('knn_interactive.py', run_name='__main__')
state = ns['state']
ns['_on_btn_test'](None); ns['_on_btn_kfold'](None); ns['_on_btn_test'](None)
assert len(state['scores']) == 3
for s in state['scores']:
    print(s)
captured['fig'].savefig('/tmp/knn_step12.png', dpi=110)
print('saved')
"
```
Expected: 3 entradas en `scores` y un PNG con bordes verde/rojo en los test points.

- [ ] **Step 4: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: botones Test completo y K-fold + scores"
```

---

## Task 13: Panel derecho con la lista de scores

**Files:**
- Modify: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Agregar referencia al texto del panel derecho cerca del setup de `ax_scores`**

Reemplazar el bloque actual de `ax_scores` por:

```python
ax_scores = fig.add_axes([0.80, 0.36, 0.18, 0.58])
ax_scores.set_xticks([])
ax_scores.set_yticks([])
ax_scores.set_facecolor("#fafafa")
ax_scores.set_title("Scores", fontsize=10, weight="bold")
scores_text = ax_scores.text(
    0.02, 0.98, "", transform=ax_scores.transAxes,
    ha="left", va="top", fontsize=9, family="monospace",
)
```

- [ ] **Step 2: En `redraw()`, después del bloque de test_scatter, actualizar el texto**

```python
    # 4. Lista de scores (más recientes arriba, hasta 12)
    lines = []
    for i, s in enumerate(state["scores"][:12]):
        n = len(state["scores"]) - i
        m = {"Euclidiana": "eucl", "Manhattan": "manh",
             "Minkowski": "mink", "Mahalanobis": "mahal"}[s["metric"]]
        w = {"uniform": "uni", "1/d": "1/d", "gaussiano": "gau"}[s["weights"]]
        lines.append(f"[{n:>2}] {s['type']:<5} k={s['k']:<2} {m:<5} {w:<3}  acc={s['score']:.3f}")
    scores_text.set_text("\n".join(lines))
```

- [ ] **Step 3: Smoke test que el panel se llena con varias evaluaciones**

Run:
```bash
cd knn_2d && MPLBACKEND=Agg uv run python -c "
import matplotlib.pyplot as plt
captured = {}
def fake(*a, **k): captured['fig'] = plt.gcf()
plt.show = fake
import runpy
ns = runpy.run_path('knn_interactive.py', run_name='__main__')
ns['_on_btn_test'](None)
ns['_on_btn_kfold'](None)
ns['_on_metric']('Manhattan')
ns['_on_btn_test'](None)
ns['_on_btn_kfold'](None)
print(ns['scores_text'].get_text())
captured['fig'].savefig('/tmp/knn_step13.png', dpi=110)
print('saved')
"
```
Expected: 4 líneas en el panel Scores, render con texto visible a la derecha.

- [ ] **Step 4: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: panel derecho con lista de scores"
```

---

## Task 14: Recuadro Control (Regenerar / Reset)

**Files:**
- Modify: `knn_2d/knn_interactive.py`

- [ ] **Step 1: Agregar después del recuadro Evaluación**

```python
# ===========================================================
# Recuadro Control
# ===========================================================
_add_group_box(0.025, 0.36, 0.21, 0.155, "Control")

ax_btn_regen = plt.axes([0.04, 0.45, 0.18, 0.04])
btn_regen = Button(ax_btn_regen, "Regenerar datos",
                    color="#d9f5d2", hovercolor="#b6e6a8")

ax_btn_reset = plt.axes([0.04, 0.39, 0.18, 0.04])
btn_reset = Button(ax_btn_reset, "Reset todo")


def _on_btn_regen(_event):
    _regenerate_data()
    state["scores"].clear()
    state["last_query"] = None
    redraw()


def _on_btn_reset(_event):
    # Resetea sliders + radios a defaults; eso dispara los callbacks que
    # regeneran datos y limpian scores cuando corresponda.
    state["scores"].clear()
    state["last_query"] = None
    state["y_test_pred"] = None
    sl_n_classes.set_val(DEFAULTS["n_classes"])
    sl_n_train.set_val(DEFAULTS["n_train"])
    sl_n_test.set_val(DEFAULTS["n_test"])
    sl_k.set_val(DEFAULTS["k"])
    sl_p.set_val(DEFAULTS["minkowski_p"])
    sl_h.set_val(DEFAULTS["gaussian_h"])
    radio_metric.set_active(METRICS.index(DEFAULTS["metric"]))
    radio_weights.set_active(WEIGHTS.index(DEFAULTS["weights"]))
    redraw()


btn_regen.on_clicked(_on_btn_regen)
btn_reset.on_clicked(_on_btn_reset)
```

- [ ] **Step 2: Smoke test de Regenerar y Reset**

Run:
```bash
cd knn_2d && MPLBACKEND=Agg uv run python -c "
import matplotlib.pyplot as plt
plt.show = lambda *a, **k: None
import runpy
ns = runpy.run_path('knn_interactive.py', run_name='__main__')
state = ns['state']

ns['_on_btn_test'](None)
assert len(state['scores']) == 1
ns['_on_btn_regen'](None)
assert len(state['scores']) == 0 and state['last_query'] is None
print('regen OK')

ns['sl_k'].set_val(13)
assert state['k'] == 13
ns['_on_btn_reset'](None)
assert state['k'] == ns['DEFAULTS']['k']
print('reset OK')
"
```
Expected: `regen OK` y `reset OK`.

- [ ] **Step 3: Commit**

```bash
git add knn_2d/knn_interactive.py
git commit -m "knn_2d: botones Regenerar datos y Reset todo"
```

---

## Task 15: README + actualizar README raíz + smoke test final

**Files:**
- Create: `knn_2d/README.md`
- Modify: `README.md`

- [ ] **Step 1: Crear `knn_2d/README.md`**

```markdown
# k-Nearest Neighbors interactivo (2D)

Demo interactivo del clasificador k-NN sobre el plano `(x1, x2) ∈ R²`.
Permite jugar con `k`, la métrica de distancia, la función de peso y el
tamaño del dataset, y ver en vivo cómo cambian la región de decisión, la
predicción de un punto cualquiera y la accuracy sobre un test set.

## Instalación

Requiere [uv](https://docs.astral.sh/uv/).

```bash
uv sync
```

## Ejecución

```bash
uv run python knn_interactive.py
```

Si tu backend de matplotlib no es interactivo:

```bash
MPLBACKEND=TkAgg uv run python knn_interactive.py
```

## Uso

**Click sobre el plano** evalúa un punto cualquiera: marca la query con
`x` y dibuja líneas a sus `k` vecinos más cercanos. El título del plot
reporta clase predicha y confianza.

**Recuadros del panel izquierdo (de arriba abajo):**

- **Vista** — checkboxes para mostrar/ocultar la región de decisión y los
  test points.
- **Evaluación** — `Eval. punto (click)` (recordatorio visual; el click
  siempre evalúa), `Test completo` (clasifica todo `X_test` y reporta
  accuracy), `K-fold CV (5)` (5-fold sobre el train).
- **Control** — `Regenerar datos`, `Reset todo`.

**Panel derecho:** lista las últimas evaluaciones (test y k-fold) con
`k`, métrica, pesos y accuracy.

**Zona inferior (sliders + recuadros):**

- Sliders sueltos: `# clases` (2-6), `# train` (10-500), `# test` (5-200), `k` (1-25).
- Recuadro **Métrica**: Euclidiana / Manhattan / Minkowski / Mahalanobis.
  El slider `p` se activa cuando elegís Minkowski.
- Recuadro **Pesos**: uniform / 1/d / gaussiano. El slider `h` se activa
  cuando elegís gaussiano.

Mahalanobis usa la covarianza global del train (un solo `Σ` para todas
las clases). Empates de votos se resuelven por menor índice de clase.
```

- [ ] **Step 2: Actualizar README raíz para listar el nuevo ejemplo**

En `README.md` raíz, en la tabla de ejemplos, sumar después de la fila de
`proceso_gausiano_2d/`:

```markdown
| [`knn_2d/`](knn_2d/) | k-Nearest Neighbors interactivo en 2D: dataset sintético multi-clase, métricas y pesos configurables, región de decisión, evaluación por click / test set / k-fold. |
```

- [ ] **Step 3: Smoke test integral final (todos los flujos)**

Run:
```bash
cd knn_2d && MPLBACKEND=Agg uv run python -c "
import matplotlib.pyplot as plt
captured = {}
def fake(*a, **k): captured['fig'] = plt.gcf()
plt.show = fake
import runpy
ns = runpy.run_path('knn_interactive.py', run_name='__main__')
state = ns['state']

# Click sobre el plot
class E: pass
e = E(); e.inaxes = ns['ax']; e.xdata = 1.0; e.ydata = 1.0; e.button = 1
ns['on_click'](e)
assert state['last_query'] is not None

# Cambiar a Mahalanobis (que recalcula VI)
ns['_on_metric']('Mahalanobis')
ns['_on_btn_test'](None)
assert state['scores'][0]['metric'] == 'Mahalanobis'

# Cambiar a Minkowski + 1/d + ajustar p
ns['_on_metric']('Minkowski')
ns['_on_weights']('1/d')
ns['_on_p'](2.5)
ns['_on_btn_kfold'](None)

# Cambiar # clases (regenera datos, limpia scores)
ns['sl_n_classes'].set_val(5)
assert state['n_classes'] == 5 and len(state['scores']) == 0

# Volver a evaluar
ns['_on_btn_test'](None)
ns['_on_btn_kfold'](None)
assert len(state['scores']) == 2

# Toggle región de decisión y test points
ns['_on_check']('Mostrar región de decisión')
ns['_on_check']('Mostrar test points')

# Reset
ns['_on_btn_reset'](None)
assert state['n_classes'] == ns['DEFAULTS']['n_classes']
assert state['k'] == ns['DEFAULTS']['k']
assert state['metric'] == ns['DEFAULTS']['metric']

captured['fig'].savefig('/tmp/knn_final.png', dpi=110)
print('TODO OK — render guardado en /tmp/knn_final.png')
"
```
Expected: `TODO OK` y un PNG final con todo el layout limpio.

- [ ] **Step 4: Commit y push (último commit incluye todo)**

```bash
git add knn_2d/README.md README.md
git commit -m "knn_2d: README del ejemplo + actualiza README raíz"
git push
```

---

## Out of scope reminders

Estas cosas NO se implementan en este plan (ya están listadas en el spec):

- Mahalanobis per-clase.
- Cross-validation con número de folds configurable.
- Exportar resultados a CSV.
- Animaciones.
- Búsqueda de hiperparámetros automática.
